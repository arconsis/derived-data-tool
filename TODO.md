# TODO

- [ ] Convert each GitHub Action to the new format with helpers function injected using `swift -i`


## Actions to Convert

- [ ] `.github/actions/add-git-tag` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/build-executable` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/create-release-branch` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/create-swift-dependency-cache` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/extract-swift-version` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/get-version` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/set-version-in-code` – Update to use `swift -i` for helpers injection
- [ ] `.github/actions/version-validation` – Update to use `swift -i` for helpers injection

Add more actions above as you enumerate them in the repository.
